# mysql-operator-values.yaml
nodeSelector:
  kubernetes.io/hostname: workerserver

# Optional: Add resource limits
resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"